apply plugin: 'base'

allprojects {
	apply plugin: 'maven-publish'

	group = 'com.akaite.warehouse.ui'
	version = property('ui.version')

	publishing {
		publications {
			modelPublish(MavenPublication)
		}
	}
}