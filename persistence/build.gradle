apply plugin: 'base'

allprojects {
	apply plugin: 'maven-publish'

	group = 'com.akaite.warehouse.persistence'
	version = property('persistence.version')

	publishing {
		publications {
			persistencePublish(MavenPublication)
		}
	}
}