apply plugin: 'base'

allprojects {
	apply plugin: 'maven-publish'

	group = 'com.akaite.warehouse.client'
	version = property('client.version')

	publishing {
		publications {
			modelPublish(MavenPublication)
		}
	}
}