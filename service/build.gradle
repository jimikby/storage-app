apply plugin: 'base'

allprojects {
	apply plugin: 'maven-publish'

	group = 'com.akaite.warehouse.service'
	version = property('service.version')

	publishing {
		publications {
			modelPublish(MavenPublication)
		}
	}
}