apply plugin: 'base'

allprojects {
	apply plugin: 'maven-publish'

	group = 'com.akaite.warehouse.model'
	version = property('model.version')

	publishing {
		publications {
			modelPublish(MavenPublication)
		}
	}
}